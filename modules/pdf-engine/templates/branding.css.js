/**
 * Branding color constants for the PDF CSS template layer.
 * Pure data — no rendering.
 */

export const branding = {
  accentColor: '#008080',
  titleColor: '#0F3D3D',
  headerColor: '#008080',
  footerColor: '#5A5A5A',
};

export const {
  accentColor,
  titleColor,
  headerColor,
  footerColor,
} = branding;

export default branding;
