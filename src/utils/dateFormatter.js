/**
 * @deprecated Import from `./formatTimestamp.js` instead.
 * Kept as a thin compatibility shim for older imports.
 */

export {
  formatTimestamp,
  formatGeneratedLabel,
  formatTimestamp as formatHeaderDateTime,
  formatTimestamp as formatGeneratedTimestamp,
} from './formatTimestamp.js'

export { default } from './formatTimestamp.js'
